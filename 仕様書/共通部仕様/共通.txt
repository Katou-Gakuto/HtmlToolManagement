システム共通仕様・プロジェクト分離定義書
1. 全体プロジェクト構成
本ソフトウェアは、役割と責任を明確に分担した3つの独立したプロジェクトで構成される
。
設定プロジェクト (ConfigProject): ユーザー設定（キーコン、テーマ、プラグイン等）の編集とJSON出力を行う
。
ゲームプロジェクト (GameProject): 整理ゲーム本体。実ファイルは変更せず、「整理予定」を生成する
。
実行プロジェクト (ExecuteProject): ゲームが出力した Result.json を読み込み、実際にファイル操作を行う
。
2. 共通基盤仕様 (Shared Library)
プロジェクト間およびユニット間での「干渉」を防ぎつつ「連携」を可能にするため、以下の要素を**Shared（共通）**として定義し、すべての開発において最優先の「正」とする
。
2.1 ID管理システム
すべてのデータ参照はメモリアドレスやオブジェクト参照ではなく、**一意のID（UUID）**を用いて行う
。
対象: File, Folder, Action, Pattern, Skill, Plugin
。
これにより、UIユニットが特定のファイルを操作する際、データユニットの内部構造を意識せず「ID」を指定するだけで連携が可能となる
。
2.2 共通データ型定義 (Types)
すべてのプロジェクト・ユニットで参照するTypeScript型定義を Shared/Types に集約する
。
FileInfo / FolderInfo: ファイル・フォルダの基本属性
。
Action: 整理操作（Move, Delete, Hold等）のデータ構造
。
JSON Schema: 各JSONファイルの必須項目や型を定義したスキーマ
。
2.3 イベント通信 (Event Bus)
システム間の直接的な依存を避け、イベント通知によって連携する
。
例：「ファイルが選択された」「Actionが追加された」などのイベントをSharedのイベント定義に従って発火させ、他ユニットはそれを購読（Subscribe）する
。
3. ゲームプロジェクト内部のユニット分割
ゲームプロジェクト内は、以下の3ユニットに分離し、互いの内部ロジックに干渉しない構成とする。
3.1 データ・ロジック基盤ユニット (Core Logic Unit)
責務: 仮想ファイルシステムの管理、Action履歴の保持、データの保存・読込
。
依存関係: UIや描画システムに依存せず、データの整合性のみに責任を持つ
。
3.2 ワープ・マップ・プレイヤーユニット (World Explorer Unit)
責務: 2Dマップ生成、プレイヤー移動、物理判定、ゲーム的演出
。
制限: ファイルの実体データは直接持たず、Coreから提供されたIDを元にオブジェクトを描画する
。
3.3 UI・支援システムユニット (UI & Support Unit)
責務: HUD、メニュー、Skill検索、Pattern提案、Plugin管理
。
分離ルール: UIは表示のみを担当し、ゲームロジックやファイルデータを直接書き換えない
。Coreユニットに対し、IDを伴うActionの発行を依頼する形をとる
。
4. プロジェクト・ユニット間連携の境界条件
干渉を完全に排除するための実装ルールを定義する。
実ファイル変更の禁止: ゲームプロジェクト内では、いかなる場合も実ファイルの移動・削除を行ってはならない
。
Result.json を唯一の成果物とする: ゲームプロジェクトの出力は Result.json のみであり、これが実行プロジェクトへの唯一のインターフェースとなる
。
Managerクラスの責務制限: 各Manager（ActionManager, PatternManager等）は自身の担当データ管理のみを行い、UIや他ユニットのコードを直接操作しない
。
設定の先行読込: ゲーム開始前に、設定プロジェクトが出力したConfig JSON（テーマ、キー設定等）をSharedのLoaderを介して読み込み、各ユニットに反映させる
。
5. 共通ディレクトリ構造 (Shared部)
Shared/
├─ Types/       # 全プロジェクト共通の型定義 (File, Action, Skill等)
├─ Constants/   # 共通定数 (デフォルトパス、Version、制限値)
├─ JSON/        # 各JSONのSchema定義
└─ Events/      # 共通イベント名の定義
この「共通部分」を各プロジェクト・ユニットが参照することで、**「内部の作り方は自由だが、外とのやり取り（口）は共通」**という状態を維持し、個別の開発と最終的な統合を両立させる。